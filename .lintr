linters:
  linters_with_tags("best_practices", "readability", "consistency", "efficiency")
  linters_with_defaults(
    object_usage_linter(
      interpret_glue = TRUE
    ),
    object_name_linter = NULL, 
    indentation_linter(
      indent = 4L
    ),
    commented_code_linter = NULL,
    line_length_linter = line_length_linter(80L)
  )
